{
  "name": "@aantonov/redis-mock",
  "version": "0.0.1",
  "description": "Redis mock for tests",
  "main": "index.js",
  "scripts": {
    "test": "mocha test",
    "coverage": "istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/aantonovdevelop/redis-mock.git"
  },
  "author": "Artem Antonov <aantonov.develop@gmail.com> (http://24develop.ru)",
  "license": "MIT",
  "devDependencies": {
    "coveralls": "^2.11.9",
    "istanbul": "^0.4.2",
    "mocha": "^2.3.4"
  }
}
